{
	"dependencies": {
		"lit": "^3.1.2"
	},
	"scripts": {
		"build": "bun build --outdir static --sourcemap=external ts/*.ts",
		"watch": "bun build --outdir static --sourcemap=external --watch ts/*.ts"
	}
}
