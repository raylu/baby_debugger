{
  "dependencies": {
    "@lit/task": "^1.0.0",
    "lit": "^3.1.2"
  },
  "scripts": {
    "build": "bun build --outdir static --sourcemap=external --spliting ts/app.ts ts/service_worker.ts",
    "watch": "bun run build --watch"
  }
}
